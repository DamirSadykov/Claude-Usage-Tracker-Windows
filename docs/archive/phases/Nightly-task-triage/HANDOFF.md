# Handoff (2026-06-24)

БАГ НОЧНОГО ТРИАЖА НАЙДЕН И ПОЧИНЕН (haiku не публиковал дайджест). Две причины: (1) allowedTools 'Bash(node *)' НЕ матчит команды 'node "путь" ...' в claude 2.1.187 - проверено 4 варианта, работает только 'Bash' целиком; этот баг блокировал И чтение доски, И публикацию; (2) даже с правами haiku не доводил агентную цепочку до 'triage publish' (капитулировал/отвлекался). РЕШЕНИЕ (выбрал пользователь: детерминированная публикация): run_triage теперь 3 шага - Rust сам экспортирует доску (node cli todos list --json -> triage-tmp/board.json), агент с allowedTools=Read+Write читает board.json и пишет triage-staging.json (Bash не нужен вовсе), Rust сам публикует (node cli triage publish --file staging). Промпт переписан (Read из <BOARD>, Write в <STAGING>, дата через <TODAY>, без CLI/Bash). Изменены: triage_schedule.rs (run_triage + resolve_node + run_node), scripts/triage-prompt.md, docs/nightly-triage.md. cargo check EXIT=0, cargo test triage_schedule 5/5. СКВОЗНОЙ прогон нового пути на haiku УСПЕШЕН: board экспортирован, агент написал дайджест, publish ok (3 items), generated_at обновился. Фронт (TodoWindow/i18n) НЕ менялся, дефолт haiku оставлен (теперь надёжен). ОСТАЛОСЬ: пересобрать приложение (Rust изменён) и проверить Run now из UI. Известный нюанс качества: haiku иногда делает headline длиннее 140 символов (промпт просит <=140) - не блокер, можно усилить позже. НЕ закоммичено.
