# Phase 1: Digest contract + CLI
<!-- status: done -->

Define triage-digest.json schema (run date, stale/overdue/no-priority lists, suggested priorities, prose summary). Add cli.mjs triage publish/show with atomic temp+rename write, mirroring todos. Mirror the struct in Rust (read-only load like todos.rs). Strictly additive - no board mutation.
