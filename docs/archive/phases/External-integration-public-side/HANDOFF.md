# Handoff (2026-07-02)

Фаза 6 Readonly-UI реализована (не верифицирована вживую в GUI — прерогатива юзера, dev-сервер не поднимал). Решение 6.1: вариант A — режим-переключатель «Мои задачи | Внешние · N» в шапке окна задач (TodoWindow.vue), внешние НЕ в канбане (статусы source-native), а отдельным readonly-списком, сгруппированным по source. 6.2: карточка = статус-пилюля (extStatusClass: done/active/open по keyword-эвристике, иначе нейтральная) + title + relTime(updated_at) + assignee/priority + внешняя ссылка ↗ (openExternal→open_url) + значок «• изменилось» для задач из свежего changes-батча этой сессии (externalChanged Set, чистится при уходе с вкладки). Данные: get_external_tasks (кэш, грузится и в local-режиме для счётчика), poll_external (ручной refresh), listen 'external-tasks-updated' (live). Empty-state: не enrolled → подсказка + кнопка в Настройки›Интеграции (openIntegrations, tab:integrations); enrolled но пусто → todoExtEmpty. i18n en+ru добавлены (todoMode*/todoExt*/todoJustNow/todoAgo). vue-tsc чисто, vitest 19 зелёных. НЕ закоммичено. СЛЕДУЮЩЕЕ (юзер): запустить resolver (cd resolver && cargo run, RESOLVER_INGEST_TOKENS=svc-a:dev-token) + трекер, привязать устройство, открыть окно задач → вкладка «Внешние», проверить рендер на 3 реальных сообщениях из mailbox; при ОК — коммит. План 6/6 фаз закрыт.
