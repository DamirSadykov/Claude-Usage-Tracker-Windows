# Нормализация переводов строк: в репозитории всегда LF,
# независимо от локального core.autocrlf (иначе Cargo.toml и др.
# периодически висят «изменёнными» с пустым diff).
* text=auto

*.png binary
*.ico binary
