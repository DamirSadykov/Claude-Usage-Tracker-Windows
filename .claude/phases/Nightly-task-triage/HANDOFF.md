# Handoff (2026-06-24)

Фаза 3 (Digest UI) готова, код проверен (vue-tsc EXIT=0, vitest 46/46). По просьбе пользователя дайджест размещён НЕ в главном окне, а в окне задач (TodoWindow.vue) - чип рядом со счётчиком активных задач (.tw-open), по клику раскрывается поповер (backdrop-закрытие) с summary, project/время и находками по группам overdue/stale/no_priority/suggestion (цветные точки). Номера #N в дайджесте кликабельны -> triageGoToTask -> openTask -> openDetail (только если номер есть в byNumber; иначе обычный текст). Загрузка get_triage_digest в onMounted + по focus + по событию triage-alert (бэкенд броадкастит во все окна). i18n: triageKind*/triageCardClean (en+ru). App.vue: карточку и её state/loader УБРАЛ, нотификация triageToast/listen('triage-alert') осталась; интерфейсы TriageDigest/DigestItem остались экспортироваться из App.vue (импортит TodoWindow). Отдельный src/components/TriageDigestCard.vue УДАЛЁН. Образец дайджеста опубликован в app data с реальными номерами (#7/#11/#22/#23/#14) - клик-переход проверяется. ВИЗУАЛЬНАЯ ПРОВЕРКА за пользователем. НЕ коммитил. СЛЕД (Фаза 4): read-only triage-промпт + Desktop scheduled task Daily @ 8:00 + e2e.
